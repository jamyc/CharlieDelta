apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: hello-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: 20.71.65.207.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: charlie-service
              servicePort: 8080
